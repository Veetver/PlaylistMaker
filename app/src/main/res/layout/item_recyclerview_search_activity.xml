<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="8dp">

    <ImageView
        android:id="@+id/artwork"
        android:layout_width="@dimen/artwork_item_search_rv_45dp"
        android:layout_height="@dimen/artwork_item_search_rv_45dp"
        tools:src="@drawable/artwork_placeholder" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:layout_marginHorizontal="8dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/track_name"
            style="@style/track_name_item_rv"
            tools:text="Yesterday (Remastered 2009)" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/artist_name"
                style="@style/details_item_rv"
                android:layout_width="56dp"
                tools:text="The Beatles" />

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:src="@drawable/dot"
                app:tint="@color/search_item_details" />

            <TextView
                android:id="@+id/track_time"
                style="@style/details_item_rv"
                android:layout_width="wrap_content"
                tools:text="2:55" />
        </LinearLayout>
    </LinearLayout>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:src="@drawable/arrow_forward"
        app:tint="@color/search_item_details" />
</LinearLayout>